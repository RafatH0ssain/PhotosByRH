/* Static imports, not string paths: Next reads each file at build time
   and supplies intrinsic dimensions plus a generated blurDataURL, so the
   grid shows a blurred preview instead of an empty box while decoding. */
import img1 from "@/public/Brand(s)/Brands (1).webp";
import img2 from "@/public/Brand(s)/Brands (2).webp";
import img3 from "@/public/Brand(s)/Brands (3).webp";
import img4 from "@/public/Brand(s)/Brands (4).webp";
import img5 from "@/public/Brand(s)/Brands (5).webp";
import img6 from "@/public/Brand(s)/Brands (6).webp";
import img7 from "@/public/Brand(s)/Brands (7).webp";
import img8 from "@/public/Brand(s)/Brands (8).webp";
import img9 from "@/public/Brand(s)/Brands (9).webp";

const images = [
  img1,
  img2,
  img3,
  img4,
  img5,
  img6,
  img7,
  img8,
  img9,
];

export default images;
